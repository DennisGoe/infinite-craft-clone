import React from "react";

const Logo = () => {
    return (
        <div className={"uppercase flex flex-col"}>
            <span className={"font-indie text-xl"}>
                infinite craft
            </span>
            <span className={"font-indie text-neutral-400"}>
                clone
                </span>
        </div>
    );
};

export default Logo;